/** @type {import('next-i18next').UserConfig} */
module.exports = {
  i18n: {
    // Daftar semua bahasa yang Anda dukung
    locales: ['en', 'id'],
    // Bahasa default yang akan digunakan
    defaultLocale: 'en',
  },
};